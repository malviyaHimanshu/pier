# Contributing to Pier

Please start with the contributor guide in [`docs/contributing.md`](docs/contributing.md).

It covers:

- local setup
- build/test commands
- generated output policy (`extension/`, `packages/*/dist`)
- compatibility expectations for `v1.x`
