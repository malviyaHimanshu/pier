{
  "name": "pier",
  "version": "1.0.0",
  "description": "Chrome extension terminal overlay for localhost pages",
  "bin": {
    "pier": "bin/pier.js"
  },
  "files": [
    "bin",
    "cli",
    "extension",
    "server",
    "scripts",
    "manifest.json",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "main": "server/pier-server.js",
  "scripts": {
    "server": "node server/pier-server.js",
    "dev-hosts": "node scripts/dev-hosts.js",
    "token": "node scripts/generate-token.js",
    "test": "node --check bin/pier.js && node --check cli/pier-utils.js && node --check cli/pier-bridge.js && node --check cli/pier-cli.js && node --check extension/content.js && node --check extension/options.js && node --check server/pier-server.js && node --check server/workspace-registry.js && node --check scripts/dev-hosts.js && node -e \"JSON.parse(require('fs').readFileSync('manifest.json','utf8'))\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-unicode11": "^0.9.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/addon-webgl": "^0.19.0",
    "@xterm/xterm": "^6.0.0",
    "node-pty": "^1.1.0",
    "ws": "^8.19.0"
  }
}
